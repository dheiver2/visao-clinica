# Interface desktop
PySide6>=6.6

# Visão computacional
opencv-python>=4.9
mediapipe>=0.10

# Numérico / séries temporais
numpy>=1.26
dask[complete]>=2024.1

# Backend de fallback do LLM (opcional — bitnet.cpp é o backend primário)
torch>=2.2
transformers>=4.44

# Persistência e relatórios
reportlab>=4.0
# sqlite3 é parte da stdlib do Python
