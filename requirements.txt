# Interface desktop
PySide6>=6.6

# Visão computacional
opencv-python>=4.9
# 0.10.14 ainda inclui a API legada solutions (Face Mesh/Pose/Hands).
# Versões >=0.10.15 removeram solutions em favor da Tasks API.
mediapipe==0.10.14

# Numérico / séries temporais / processamento de sinal
numpy>=1.26
scipy>=1.11          # Welch PSD, Butterworth, filtros (precisão dos biomarcadores)
dask[complete]>=2024.1

# Backend de fallback do LLM (opcional — bitnet.cpp é o backend primário)
torch>=2.2
# BitNet em CPU regrediu no transformers 5.x (pesos ternários não desempacotam);
# a linha 4.x é a validada para o fallback.
transformers>=4.50,<5
accelerate>=0.30   # exigido pelo quantizador BitNet no fallback transformers

# Persistência e relatórios
reportlab>=4.0
# sqlite3 é parte da stdlib do Python
