# Swift / SwiftPM
.build/
.swiftpm/
Package.resolved

# App bundle / distribuição
dist/
*.dmg

# Dados do usuário (gerados em ~/Library/Application Support/VisaoClinica)
data/
*.db

# Modelos (não versionar — pesados; o app nativo usa o framework Vision)
models/
*.gguf
*.task
vendor/

.DS_Store

# Resíduos do app Python legado (não versionar se reaparecerem localmente)
__pycache__/
*.pyc
.venv/
venv/
